# Ralph Progress Log
Started: Tue Mar  3 14:29:53 -03 2026
---

## Codebase Patterns
- `_extract_processor_charges(payment)` returns 5-tuple: `(mp_fee, shipping_cost_seller, shipping_id, reconciled_net, net_diff)`
- `_upsert_payment()` accepts optional `processor_fee`, `processor_shipping`, `error`, `ca_evento_id` kwargs
- Pre-existing lint error on processor.py line 508 (f-string without placeholders) — not introduced by Ralph
- `get_missing_ca_launch_fields(seller)` checks for `ca_conta_bancaria` and `ca_centro_custo_variavel`
- `daily_sync.py` `should_reprocess` tuple on line 349 is the gate for re-sending known payments to processor — add new reprocessable statuses there
- `onboarding_backfill.py` `_load_already_done()` `.in_("status", [...])` on line 561 controls which payments are skipped — new statuses are excluded by default
- `main.py` lifespan: `nightly_pipeline_enabled` → single pipeline scheduler; `else` → individual schedulers (baixa, sync, closing, legacy). Baixas are step 5 in nightly pipeline, no standalone scheduler.

---

## 2026-03-03 - US-001
- Changed processor.py early-return block (lines 224-243) to save `pending_ca` instead of `skipped` when seller lacks CA config
- Computed `processor_fee` and `processor_shipping` via `_extract_processor_charges()` and passed to `_upsert_payment()`
- Files changed: `app/services/processor.py`
- **Learnings for future iterations:**
  - The `_upsert_payment` function always saves `raw_payment` (the full ML payment dict) — no extra work needed
  - The error field format is `missing_ca_config:field1,field2` for traceability
  - Only the `if missing_ca_fields:` block needed changing; everything downstream (approved flow, refund flow) is untouched
---

## 2026-03-03 - US-002
- Added `"pending_ca"` to the `should_reprocess` tuple in `daily_sync.py` line 349
- This ensures payments with `processor_status='pending_ca'` are reprocessed each nightly sync
- If seller now has CA config, processor will create ca_jobs and set status=queued
- If seller still lacks CA config, processor saves as pending_ca again (idempotent)
- Files changed: `app/services/daily_sync.py`
- **Learnings for future iterations:**
  - `existing_orders` query loads ALL payments for the seller (no status filter), so no change needed there
  - The `should_reprocess` logic is the single gate controlling whether a known payment gets re-sent to `process_payment_webhook()`
  - The change is minimal: just one tuple expansion + one comment line
---

## 2026-03-03 - US-003
- Verified `_load_already_done()` in onboarding_backfill.py does NOT include `pending_ca` in the done set
- The `.in_("status", [...])` filter on line 561 lists: `synced, queued, refunded, skipped, skipped_non_sale` — `pending_ca` is absent
- Updated docstring to explicitly document that `pending_ca` payments are re-evaluated by the backfill
- Files changed: `app/services/onboarding_backfill.py`
- **Learnings for future iterations:**
  - `_load_already_done()` is the single gate for backfill resumability — only statuses in its `.in_()` list are skipped
  - Since `pending_ca` is a new status, it was already excluded by default — the change was purely a docstring clarification
  - The backfill also loads from `mp_expenses` table (non-order payments), but that table has no status filter concern for `pending_ca`
---

## 2026-03-03 - US-004
- Verified nightly pipeline baixa timing: when `nightly_pipeline_enabled=true`, `_daily_baixa_scheduler` is NOT started (it's in the `else` branch of `lifespan`)
- Verified `_run_nightly_pipeline()` runs `_run_baixas_all_sellers()` as step 5 sequentially after steps 1-4
- Verified onboarding backfill calls `_trigger_baixas()` as step 7, inline with no time dependency
- Added clarifying comments in `lifespan()` (lines 357-362) and at step 5 of `_run_nightly_pipeline()` (lines 274-275)
- Files changed: `app/main.py`
- **Learnings for future iterations:**
  - `lifespan()` has a clear `if/else` on `nightly_pipeline_enabled`: nightly mode starts ONE scheduler; legacy mode starts individual schedulers
  - The `_daily_baixa_scheduler` (10h BRT) only runs in legacy mode — when nightly pipeline is active, baixas are step 5 inside the pipeline
  - Onboarding backfill is independent of both schedulers — it triggers baixas inline via `_trigger_baixas()`
---
